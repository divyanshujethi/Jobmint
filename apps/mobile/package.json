{
  "name": "mobile",
  "version": "0.1.0",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "check-types": "tsc --noEmit",
    "build:apk": "eas build --platform android --local --profile preview"
  },
  "dependencies": {
    "@repo/matching": "workspace:*",
    "@repo/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}